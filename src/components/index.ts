export { default as Rain } from "./Rain/Rain";
// This page can be used to set list of multiple export components so the 
// user will only have to import by 'import {Rain} from 'react-rainfall' instead of 
// having to specify the entire path to the export default component 